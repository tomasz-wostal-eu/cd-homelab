---
# ExternalSecret for hydrohabitat repository
# Syncs SSH key from Azure Key Vault to ArgoCD repository secret
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-repo-hydrohabitat
  namespace: argocd
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-store
    kind: ClusterSecretStore
  target:
    name: argocd-repo-hydrohabitat
    creationPolicy: Owner
    template:
      metadata:
        labels:
          argocd.argoproj.io/secret-type: repository
      data:
        type: git
        url: git@github.com:tomasz-wostal-eu/hydrohabitat.git
        sshPrivateKey: "{{ .sshPrivateKey }}"
  data:
    - secretKey: sshPrivateKey
      remoteRef:
        key: argocd-hydrohabitat-ssh-key
