---
# DNSEndpoint for External DNS - creates CNAME to Cloudflare Tunnel
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: argo-workflows-dns
  namespace: argo-workflows
  annotations:
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  endpoints:
    - dnsName: workflows.devopslaboratory.org
      recordTTL: 300
      recordType: CNAME
      targets:
        - d5854df6-8267-4a84-8d51-3431d4c0c15d.cfargotunnel.com
---
# HTTPRoute for Argo Workflows via Cloudflare Tunnel
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argo-workflows
  namespace: argo-workflows
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: istio-system
      group: gateway.networking.k8s.io
      kind: Gateway
  hostnames:
    - workflows.devopslaboratory.org
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: argo-workflows-server
          port: 2746
          group: ""
          kind: Service
          weight: 1
