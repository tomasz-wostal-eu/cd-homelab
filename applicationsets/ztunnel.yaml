---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ztunnel
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - name: homelab
            cluster: https://kubernetes.default.svc
            namespace: istio-system
            helmRevision: "1.24.*"
            env: homelab
            region: local
            type: homelab
  template:
    metadata:
      name: ztunnel-{{ .name }}
      labels:
        app.kubernetes.io/name: ztunnel
        app.kubernetes.io/instance: ztunnel-{{ .name }}
        env: "{{ .env }}"
        cluster: "{{ .name }}"
        region: "{{ .region }}"
        type: "{{ .type }}"
      annotations:
        homelab.argocd.io/cluster-url: "{{ .cluster }}"
        argocd.argoproj.io/sync-wave: "2"
    spec:
      project: platform-networking
      sources:
        - repoURL: https://istio-release.storage.googleapis.com/charts
          chart: ztunnel
          targetRevision: "{{ .helmRevision }}"
          helm:
            releaseName: ztunnel
            parameters:
              - name: global.platform
                value: k3s
            valueFiles:
              - $values/values/ztunnel/common/values.yaml
              - $values/values/ztunnel/{{ .region }}/{{ .env }}/values.yaml
        - repoURL: ssh://git@forgejo.tailc90e09.ts.net/tomasz-wostal-eu/cd-homelab.git
          targetRevision: main
          ref: values
      destination:
        server: "{{ .cluster }}"
        namespace: "{{ .namespace }}"
      revisionHistoryLimit: 5
      info:
        - name: Region
          value: "{{ .region }}"
        - name: Environment
          value: "{{ .env }}"
        - name: Type
          value: "{{ .type }}"
        - name: Chart Version
          value: "{{ .helmRevision }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          limit: 5
          backoff:
            duration: 15s
            factor: 2
            maxDuration: 10m
        managedNamespaceMetadata:
          labels:
            owner: argocd
            app.kubernetes.io/part-of: istio
            region: "{{ .region }}"
            env: "{{ .env }}"
          annotations:
            argocd.argoproj.io/managed-by: "ztunnel-{{ .name }}"
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - PruneLast=true
          - ApplyOutOfSyncOnly=true
          - PrunePropagationPolicy=foreground
          - RespectIgnoreDifferences=true
