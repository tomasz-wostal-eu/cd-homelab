---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: platform-delivery
  namespace: argocd
spec:
  description: Platform delivery (Kargo, Argo Rollouts)

  sourceRepos:
    - '*'

  destinations:
    - namespace: '*'
      server: '*'

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'
