# Mimir homelab-specific configuration
# Single binary mode - all in one pod

# Override resources for homelab
mimir_monolithic:
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 4Gi
  persistentVolume:
    size: 10Gi
